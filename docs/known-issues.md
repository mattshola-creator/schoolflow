# Known Issues

## Open

- The connected Supabase organization exposes no development project. Remote migration, advisors, type generation and connectivity verification cannot run without a project ID.
- The canonical GitHub repository exists but is empty and public. The local Git client has no authenticated push credential, so the verified commit history has not been published and CI has not run.
- Netlify is connected but has no SchoolFlow project. GitHub-connected deployment is blocked by the empty repository and missing Supabase public configuration.

## Closed

- None.
