# Known Issues

## Open

- The Supabase organization is connected but contains no development project. Remote migration and connectivity verification are blocked until a project is created or connected.
- The GitHub connection is healthy but the account has no repository. The connector cannot create repositories, so the canonical repository must be created before push/CI verification.
- Netlify is connected but project creation and deployment are blocked on the GitHub repository and Supabase public configuration.

## Closed

- None.
